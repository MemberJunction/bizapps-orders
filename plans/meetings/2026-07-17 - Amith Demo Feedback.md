Amith Demo Feedback
Nice improvements, Accounting UX is looking very close to production caliber IMO  
Beyond the dashboard UI for Journal Entries, I think we need to make sure the forms for each of those entities are very nice so when you drill into those forms, they look the same. I suggest you make the forms all have widgets that can be reused directly on the dashboards so it is truly one UX. Claude knows how to do this, as the pattern is what we do in many other places. 
Manual JEs - interesting concept, I'm surely not against this but we have to be careful to ensure the provenance for all JEs is super clear, those coming from Orders/Payments/other apps have that clear lineage for manual ones we'd need to ensure authorization for creating/approving them and the fact that they're manual is super clear. Normally manual entries are done in the Accounting system, not in our type of env but I think having this in our world is fine subject to earlier bits.
Marcelo Torres your question for me on Multi-Company Batches. The short answer is we should think through this with Robert Kihm. One option is to allow this and then split them into per-company-sections or similar inside the batch where you aggregate by company. Another option would be to have source journal entries split so they can go into multiple-batches (split by company), the latter seems messy though. I think therefore that we probably will need to support batches spanning multiple-companies/entities and properly aggregate by company/entity within the batch and when we actually push the data over to the GL system you're doing one journal entry per company. Robert Kihm and Jeremy Hunnewell your thoughts on this issue?
Accounts/Chart of Accounts tab on the dashboard - I think you could probably re-use the ng-entity-viewer with a User View in situations like that instead of rebuilding that UX functionality from scratch in situations like this. 
Don't put too much more work into this, it is a good starting point and we can improve based on user feedback, many other things to focus on beyond this.
For the rest, the main comment I have for Orders/Payments/Subs - same comment as above, build Entity Forms for each that are first class and use those entity forms from the dashboard, not custom pop-up things like you have. That way the forms can be accessed however we want them in various places, that is how MJE was designed with reusable core widgets, use those instead of custom things all over.
Will let Robert work with you in detail on all the flows for Products/Orders/Payments/Subs.