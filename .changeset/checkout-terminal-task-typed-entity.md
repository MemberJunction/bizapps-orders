---
'@mj-biz-apps/orders-core-entities-server': patch
---

Type the checkout-capture terminal Task through `@mj-biz-apps/tasks-entities` (typed `TypeID`/`Name`/`Status` setters and TaskType.ID getter) instead of untyped `.Set()` / `.Get()`.
