---
'@mj-biz-apps/orders-integration-tests': patch
---

Add integration coverage for the entitlement read contract: in-process `Orders.CheckEntitlement` / `ListEntitlements` (ER1–ER7) and the same operations over GraphQL `ExecuteRemoteOperation` (WE1–WE5).
