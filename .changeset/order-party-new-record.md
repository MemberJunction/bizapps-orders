---
"@mj-biz-apps/orders-ng": patch
---

Order header Ship To sits on the left and starts expanded; Bill To is a slim rail that takes the width when selected. Person and Organization Orders grids pass every filter join field as NewRecordValues so a new order is linked back to the party.
